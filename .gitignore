# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions
 
# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem
__MACOSX

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# env files (ignore all real env files; the *.example templates ARE committed)
.env*
!.env.example

# embedded postgres (dev)
/.pgdata
/.kanbanica-postgres
# legacy data dir from before the Krova → Kanbanica rename; still reused when present
/.krova-postgres

# local file storage (fs adapter) — uploaded attachments/images in dev
/uploads

# typescript
*.tsbuildinfo
next-env.d.ts

# database dumps
*.sql.gz
*.sql.zst
*.dump
kanbanica-db-*
krova-db-*

# daisyUI migration Phase 0 baseline screenshots/token dumps (QA scaffolding, not app code)
/.claude-migration-baselines-phase0
